# @summary To set up PS1 variable so command prompt is standardised
#
# Set the PS1 variable to allow the command prompt to be as useful as posible
# such as by adding colour and full path to working directory 
#
# @example
#   include ps1_prompt
class ps1_prompt {
  include ps1_prompt::config
}
